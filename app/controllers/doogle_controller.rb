class DoogleController < ApplicationController
  def search
  end

  #def process_entry
    #looks get entry from local database
    #if there is a record use it and show definitions
    #else call dictionary api to look up word
    #if found post new word and definitions then show them to user
    #else let user know the word does not exist
  #end

  #def get_entry
    #returns a word's definitions
    #otherwise returns null
  #end

  #def post_entry
    #add new word and definitions to database
  #end
end
